# 👁️ OCR gRPC Service

![Python](https://img.shields.io/badge/Python-3.13%2B-blue)
![gRPC](https://img.shields.io/badge/gRPC-Service-green)
![PaddleOCR](https://img.shields.io/badge/PaddleOCR-v3-orange)
![Docker](https://img.shields.io/badge/Docker-Supported-blue)

Высокопроизводительный микросервис для распознавания текста (OCR) на изображениях. Построен на базе **PaddleOCR** и работает через протокол **gRPC**. Поддерживает ускорение на **GPU** и запуск на **CPU**.

---

## 📂 Структура проекта

```text
ocr-service/
├── proto/               # Исходные файлы протокола (.proto)
├── src/
│   ├── generated/       # Автоматически сгенерированный код gRPC
│   └── server/          # Основная логика сервера
│       ├── server.py    # Логика OCR и сервиса
│       └── __main__.py  # Точка входа
├── scripts/             # Скрипты запуска (start.sh)
├── requirements.txt     # Зависимости приложения
├── Dockerfile           # Конфигурация сборки образа (GPU и CPU)
└── README.md            # Документация
```

---

## 🛠️ Локальная установка и запуск

### 1. Предварительные требования

* Python 3.13+
* (Опционально) NVIDIA GPU + CUDA 11.8+ для ускорения

### 2. Установка зависимостей

Рекомендуется использовать виртуальное окружение:

```bash
# Создание venv (Windows)
python -m venv venv
.\venv\Scripts\Activate.ps1

# Создание venv (Linux/Mac)
python3 -m venv venv
source venv/bin/activate
```

Установите зависимости:

```bash
pip install -r requirements.txt
```

**Примечание:** Если у вас есть GPU, установите GPU-версию PaddlePaddle дополнительно:

```bash
python -m pip install paddlepaddle-gpu==3.2.2 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
```

### 3. Генерация gRPC кода (Proto)

Если вы изменили файл `proto/ocr.proto`, пересоберите Python-файлы:

```bash
python3 -m grpc_tools.protoc \
    -I proto \
    --python_out=src/generated \
    --grpc_python_out=src/generated \
    proto/ocr.proto
```

### 4. Запуск сервера

По умолчанию сервер пытается использовать GPU.

**Запуск с GPU (по умолчанию):**
```bash
python -m src.server
```

**Запуск на CPU (если нет видеокарты):**
*Windows (PowerShell):*
```powershell
$env:OCR_DEVICE="cpu"; python -m src.server
```
*Linux/Mac:*
```bash
OCR_DEVICE=cpu python -m src.server
```

Сервер запустится на порту `50051`.

---

## 🐳 Запуск в Docker

Это рекомендуемый способ для продакшена. Образ базируется на официальном `paddlepaddle` с поддержкой GPU и CPU.

### 1. Сборка образа

#### Для GPU:

```bash
docker build --build-arg DEVICE=gpu -t ocr-service:gpu .
```

#### Для CPU:

```bash
docker build --build-arg DEVICE=cpu -t ocr-service:cpu .
```

### 2. Запуск контейнера

#### GPU версия (требуется NVIDIA Container Toolkit):

```bash
docker run -d --gpus all -p 50051:50051 --name ocr-service ocr-service:gpu
```

#### CPU версия (работает везде):

```bash
docker run -d -p 50051:50051 --name ocr-service ocr-service:cpu
```

### 3. Проверка логов

```bash
docker logs ocr-service
```

Должно вывести:

```text
[INFO] Initializing PaddleOCR with device: gpu
[INFO] PaddleOCR initialized successfully.
[INFO] Starting gRPC server...
[INFO] gRPC server started on port 50051
```

---

## 📋 Зависимости (Requirements)

### requirements.txt

Только библиотеки приложения. `paddlepaddle` уже есть в базовом Docker-образе, но для локального запуска его нужно ставить отдельно (см. пункт 2 установки).

```text
paddleocr==3.3.2
opencv-python
pillow
grpcio==1.76.0
grpcio-tools==1.76.0
```

---

## 📝 Логирование

Логи настроены на ротацию и пишутся в `logs/ocr_service.log`:

* **Размер файла:** 10 МБ максимум
* **Количество архивов:** 5 предыдущих файлов хранится
* **Старые логи:** Автоматически удаляются

### Просмотр логов

```bash
# В реальном времени
tail -f logs/ocr_service.log

# Последние 100 строк
tail -100 logs/ocr_service.log
```

---

## 🔧 Переменные окружения

При запуске контейнера или локально можно использовать:

* `OCR_DEVICE` — тип устройства (`cpu` или `gpu`).
* `CUDA_VISIBLE_DEVICES` — для управления видеокартами (если нужно).

Пример явного запуска контейнера на CPU:

```bash
docker run -d \
  -e OCR_DEVICE=cpu \
  -e CUDA_VISIBLE_DEVICES="" \
  -p 50051:50051 \
  --name ocr-service \
  ocr-service:cpu
```

---

## 📝 Заметки

* Первый запрос может выполняться дольше обычного из-за инициализации модели (прогрев — ~5 сек).
* Логи пишутся в `logs/ocr_service.log` и STDOUT одновременно.
* При использовании GPU на хосте должен быть установлен **NVIDIA Container Toolkit**.
* CPU версия работает везде, но медленнее (~2-3x), чем GPU версия.
