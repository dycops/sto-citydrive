# 👁️ OCR gRPC Service

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![gRPC](https://img.shields.io/badge/gRPC-Service-green)
![PaddleOCR](https://img.shields.io/badge/PaddleOCR-v3-orange)
![Docker](https://img.shields.io/badge/Docker-Supported-blue)

Высокопроизводительный микросервис для распознавания текста (OCR) на изображениях. Построен на базе **PaddleOCR** и работает через протокол **gRPC**. Поддерживает ускорение на **GPU**.

---

## 📂 Структура проекта

```text
ocr-service/
├── proto/               # Исходные файлы протокола (.proto)
├── src/
│   ├── generated/       # Автоматически сгенерированный код gRPC
│   └── server/          # Основная логика сервера
│       ├── server.py    # Логика OCR и сервиса
│       └── __main__.py  # Точка входа
├── scripts/             # Скрипты запуска (start.sh)
├── requirements.txt     # Зависимости для Docker (Prod)
├── requirements-dev.txt # Зависимости для локальной разработки
├── Dockerfile           # Конфигурация сборки образа
└── README.md            # Документация
```

---

## 🛠️ Локальная установка и запуск

### 1. Предварительные требования
*   Python 3.8+
*   (Опционально) NVIDIA GPU + CUDA 11.8 для ускорения

### 2. Установка зависимостей

Рекомендуется использовать виртуальное окружение:

```bash
# Создание venv (Windows)
python -m venv venv
.\venv\Scripts\Activate.ps1

# Создание venv (Linux/Mac)
python3 -m venv venv
source venv/bin/activate
```

Установите зависимости (выберите нужный файл):

*   **Для локальной разработки (с установкой Paddle):**
    ```bash
    pip install -r requirements-dev.txt
    ```
*   *Примечание: Если у вас есть GPU, убедитесь, что установлен `paddlepaddle-gpu`.*

### 3. Генерация gRPC кода (Proto)

Если вы изменили файл `proto/ocr.proto`, нужно пересобрать Python-файлы. Выполните команду из корня проекта:

```bash
python -m grpc_tools.protoc -I proto --python_out=src/generated --grpc_python_out=src/generated proto/ocr.proto
```

### 4. Запуск сервера

```bash
python -m src.server
```

Сервер запустится на порту `50051`.

---

## 🐳 Запуск в Docker

Это рекомендуемый способ для продакшена. Образ базируется на официальном `paddlepaddle` с поддержкой GPU.

### 1. Сборка образа

```bash
docker build -t ocr-service .
```

### 2. Запуск контейнера

С пробросом GPU (требуется NVIDIA Container Toolkit):

```bash
docker run -d --gpus all -p 50051:50051 --name ocr-service ocr-service
```

Без GPU (только CPU):

```bash
docker run -d -p 50051:50051 --name ocr-service ocr-service
```

---

## 📋 Зависимости (Requirements)

Проект использует разделение зависимостей:

1.  **`requirements.txt`** — только библиотеки приложения (`paddleocr`, `grpcio` и т.д.). Используется в Docker, так как `paddlepaddle` уже есть в базовом образе.
2.  **`requirements-dev.txt`** — включает `paddlepaddle-gpu` для запуска на Windows/Linux машине разработчика.

---

## 📝 Заметки

*   Первый запрос может выполняться дольше обычного из-за инициализации модели ("прогрев").
*   Логи пишутся в STDOUT.
